# 钉钉违规检测工具 — 运行时依赖
# Python 3.11+
# 安装: pip install -r requirements.txt

# GUI 框架
flet>=0.85,<0.86

# flet 桌面端运行时（独立包，PyInstaller 打包必需）
flet-desktop>=0.85,<0.86

# HTTP（企业微信 Webhook / 智能表上报）
requests>=2.30

# 剪贴板（GUI 复制路径，try/except 兜底，可选）
pyperclip>=1.8
